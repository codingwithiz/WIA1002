package T2Q4;
//What is erasure? Why are Java generics implements using erasure?
public class T2Q4 {
    // Erasure is the process of removing type information from generic types, so that they can be used in pre-JDK 5 code.
    // Erasure is necessary to ensure backward compatibility with pre-JDK 5 code.
    // Erasure is also necessary to ensure that generic types can be used in pre-JDK 5 code.
    // Erasure is necessary to ensure that generic types can be used in pre-JDK 5 code.
}
